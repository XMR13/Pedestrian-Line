# Jetson TensorRT runtime dependencies.
#
# Use this for the AI-box deployment path, especially when JetPack provides
# TensorRT/OpenCV through system packages. It intentionally avoids the desktop
# ONNXRuntime pin from pyproject.toml.
#
# Recommended:
#   python3 -m venv --system-site-packages .venv
#   . .venv/bin/activate
#   python -m pip install -r requirements-jetson.txt
#   python -m pip install -e . --no-deps

numpy<2; python_version < "3.9"
numpy>=1.24; python_version >= "3.9"
pyyaml>=6.0
tqdm>=4.67
fastapi>=0.115,<1
httpx>=0.27,<1
uvicorn>=0.30,<1
jinja2>=3.1,<4
cuda-python<13

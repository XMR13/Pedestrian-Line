# Normalize text files to LF in the repo to avoid CRLF/LF churn across Windows/WSL/Linux.
* text=auto eol=lf

# Windows-specific scripts keep CRLF if ever added.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
